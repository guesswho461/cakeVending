const initState = {
  selectedPage: "main",
  lastPage: "main",
  selectedIdx: 0
};

export default function reducer(state = initState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
